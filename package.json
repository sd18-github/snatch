{
  "name": "snatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "pnpm --filter @snatch/shared run build",
    "dev": "concurrently -n shared,server,client -c blue,green,magenta \"pnpm --filter @snatch/shared run dev\" \"pnpm --filter @snatch/server run dev\" \"pnpm --filter @snatch/client run dev\"",
    "build": "pnpm --filter @snatch/shared run build && pnpm --filter @snatch/server run build && pnpm --filter @snatch/client run build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "concurrently": "^9.1.2",
    "eslint": "^9.25.1",
    "typescript-eslint": "^8.31.1"
  }
}
